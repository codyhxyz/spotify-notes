# Demo video

The full shot list, recording instructions, and post-processing recipe live in [`screenshots/SHOTLIST.md`](../screenshots/SHOTLIST.md). This file is just a marker so the `video/` directory exists in source control and the README's reference target is real.

The recorded `demo.mp4` is gitignored (binary, large, regeneratable). Stash a copy somewhere durable (Drive / 1Password attachments) so re-shooting it isn't always a project.
